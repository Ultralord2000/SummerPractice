 
 import java.io.File;
import java.io.FileOutputStream;

	import com.itextpdf.kernel.pdf.PdfDocument; 
	import com.itextpdf.kernel.pdf.PdfWriter; 
	import com.itextpdf.layout.Document;  
	

	public class create_PDF {    
	   public static void main (String args []) выдает исключение {              
	      // Создание PdfWriter       
	      Строка dest = "C: /itextExamples/sample.pdf";       
	      PdfWriter writer = новый PdfWriter (dest); 
	   
	      // Создание PdfDocument       
	      PdfDocument pdfDoc = новый PdfDocument (писатель);              
	   
	      // Добавляем новую страницу 
	      pdfDoc.addNewPage ();               
	   
	      // Создание документа        
	      Документ документ = новый документ (pdfDoc);               
	   
	      // Закрытие документа    
	      document.close ();              
	      System.out.println («PDF-файл создан»);    
	   } 
	}

